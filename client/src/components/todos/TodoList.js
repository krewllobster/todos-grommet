import React from 'react'
import SingleTodo from './SingleTodo'

const TodoList = ({todos}) => {

  return (
    <div>
      Looks like you don't have any todo lists, try adding one on the left!
    </div>
  )
}

export default TodoList
